# Long-Term Memory

This file is your curated long-term memory. It is injected into your context on every session.
Use `[MEMFILE: fact]` tags in your responses to append new entries automatically.

Copy this to `~/.claude-code-telegram/config/memory.md` and edit to taste.

---

## Facts

- Example: The main project is located at ~/Projects/myapp
- Example: Preferred stack is Python + FastAPI + PostgreSQL

## Preferences

- Example: Always use type hints in Python code
- Example: Prefer uv over pip for package management

